# 멍냥 서바이버즈 v1.18 — 산책줄 / 발톱 슥삭 수정표

## 산책줄 휘리릭

### Anchor

산책줄은 캐릭터 중심이나 머리에 꽂지 않고 각 PNG의 목/등 뒤에 가까운 지점을 사용한다.

| 캐릭터 | leashAnchor |
|---|---|
| 찌개 | x -3 / y +8 |
| 만두 | x -3 / y +8 |
| 감자 | x -2 / y +9 |
| 구찌 | x -4 / y +9 |

줄 texture의 origin은 `(0.04, 0.5)`, depth는 9이고 플레이어는 depth 10이므로 연결부가 얼굴 위를 계속 가로지르지 않는다. 그래픽은 단순 rectangle이 아니라 휘어진 줄 + 끝 손잡이 형태다.

### Lv.1~Lv.5

| Lv | 줄 수 | 반경 | 회전값 | 피해 배율 | 추가 기능 |
|---:|---:|---:|---:|---:|---|
| 1 | 1 | 74px | 0.0024 | 30% | 짧은 산책줄 1개 |
| 2 | 1 | 94px | 0.0025 | 33% | 길이·범위 증가 |
| 3 | 2 | 98px | 0.0026 | 35% | 반대편 두 번째 줄, 두 줄 길이 동일 |
| 4 | 2 | 112px | 0.0032 | 38% | 두 줄 모두 성장, 일반 적 밀치기 6 |
| 5 | 2 | 128px | 0.0035 | 42% | 두 줄 모두 완성형, 밀치기 8, 3.3초마다 대환장 산책 큰 휘두르기 |

### 한 줄만 길어지던 문제 해결

Lv.3 이상에서 두 entity가 서로 다른 `outerRadius`를 쓰지 않는다. 현재 skill level에서 읽은 동일한 `st.radius`를 매 프레임 두 줄 모두에 적용한다. 레벨이 바뀌면 두 entity 모두 같은 데이터를 공유하고 필요 시 orbiters를 재생성한다.

Lv.5의 개성은 한 줄만 더 길게 만드는 것이 아니라 `큰 휘두르기`를 추가하는 방식으로 옮겼다.

## 발톱 슥삭

그래픽은 한 장짜리 사다리꼴/검기가 아니라 `clawSlash` texture 하나에 **서로 분리된 3개의 평행한 할퀸 선**을 그린다. 각 자국은 어두운 외곽선 + 밝은 안쪽 선 + 뾰족한 끝으로 구성된다.

공격 이미지는 캐릭터 중심이 아니라 공격 방향의 몸 바깥에서 시작하고 Lv.5에서는 짧게 바깥으로 이동한다.

| Lv | 유효 기본 사거리 | 방향 | 피해 배율 | 탄 제거 | 시각/기능 변화 |
|---:|---:|---:|---:|---|---|
| 1 | 62px | 위/아래 2 | 178% | X | 3줄 할퀴기 기본형 |
| 2 | 80px | 위/아래 2 | 190% | X | 길이 약 29% 증가 |
| 3 | 88px | 상/하/좌/우 4 | 202% | X | 십자 방향 모두 3줄 |
| 4 | 102px | 8방향 | 212% | 일반 `enemy` 탄 최대 12개 | 대각선 추가 |
| 5 | 128px | 8방향 | 225% | 일반 `enemy` 탄 최대 18개 | 바깥으로 이동하는 8방향 참격파 + 3줄 claw mark 원형 마무리 |

Lv.5 마무리는 generic 검 모양 ring을 사용하지 않고 플레이어 주변 8지점에서 3줄 발톱 자국이 휘감듯 퍼지는 연출을 사용한다. 실제 마무리 피해 범위는 `reach + 24px`에 적 hurtbox 여유를 더한다.

### TRUE BOSS 대응

발톱/산책줄은 TRUE BOSS의 개별 hurtbox를 대부분 인정하도록 별도 melee padding을 적용한다. 다만 허공까지 맞는 수준으로 hitbox 전체를 무조건 더하지 않고 각 기술별 약 82~90%만 사용한다.

### 탄막 무력화 제한

발톱 Lv.4~5가 지우는 것은 `kind === enemy`인 일반 적 탄환뿐이다. `boss` / `raid` 탄환은 제거하지 않으므로 TRUE BOSS 탄막을 무력화하지 않는다.
