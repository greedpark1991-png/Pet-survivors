# 멍냥 서바이버즈 v1.16.1 Visual Polish 테스트 보고서

## 목적

v1.16 실제 브라우저 테스트에서 발견된 캐릭터 픽셀 변형, HUD 텍스트 흐림, HUD 모서리 정렬, 공원 배경의 사각 타일/대형 광장 느낌을 수정했습니다. 전투 밸런스와 게임 시스템은 변경하지 않았습니다.

## 1. 캐릭터 찌그러짐 원인과 수정

### 확인된 원인

- 원본 플레이어 PNG는 네 장 모두 64×64이며 파일 자체 비율 문제는 없었습니다.
- v1.16에서는 캐릭터별로 `1.07 / 1.14 / 1.16` 같은 소수점 `worldScale`을 적용했습니다. nearest-neighbor 픽셀아트는 이런 애매한 배율과 브라우저의 Phaser FIT 확대가 겹치면 원본 픽셀 열/행이 불균등하게 복제되어 얼굴이 납작하거나 늘어난 것처럼 보일 수 있습니다.
- 로비 프리뷰는 별도 Canvas 크기에 맞춰 다시 그리는 코드가 있어 원본과 전투 렌더링 경로가 서로 달랐습니다.

### 수정

- 모든 플레이어 `worldScale = 1`.
- 플레이어 생성 시 `setScale(singleValue)`만 사용.
- player sprite에 독립적인 `displayWidth/displayHeight/setDisplaySize` 사용 없음.
- texture filter `NEAREST`.
- `pixelArt:true`, `antialias:false`, `roundPixels:true`, `autoRound:true`, camera `setRoundPixels(true)` 유지.
- 로비/ESC 프리뷰는 `<img>` + `height / width:auto` + `image-rendering:pixelated`.
- 감자 14×14, 구찌 14×14, 만두 13×13, 찌개 14×14의 기존 피격판정은 그대로 유지.

## 2. HUD 흐림 원인과 수정

### 원인

기존 전투 HUD는 960×540 Phaser Canvas 내부의 Phaser Text였습니다. 브라우저 해상도에 맞춰 Canvas 자체가 FIT 확대될 때 HUD 텍스트도 함께 nearest-neighbor 리샘플링되어 한글과 작은 숫자가 흐릿하거나 뭉쳐 보였습니다.

### 수정

고정 HUD를 HTML/CSS overlay로 이동했습니다. Phaser는 HP/XP/시간/WAVE/KILL/보스 상태 값을 약 100ms 단위로 DOM에 반영하고, 브라우저가 화면 실제 CSS 해상도에서 글자를 직접 렌더링합니다. 게임 월드의 캐릭터/적/탄환/아이템은 기존 Phaser Canvas 그대로입니다.

공통 safe area:

- `--hud-safe-x: clamp(18px, 1.45vw, 28px)`
- `--hud-safe-y: clamp(18px, 1.45vh, 28px)`
- 작은 화면은 12px fallback
- 1800px 이상에서는 좌상단 HUD와 시간/빌드 슬롯을 약 10% 정도 확대

## 3. 우측 하단 사운드 UI

- 항상 펼쳐진 슬라이더가 아니라 작은 원형 음표 버튼 1개만 표시.
- 우측/하단 모두 공통 safe area 사용.
- 클릭하면 버튼 위쪽으로 BGM/SFX popover가 열림.
- 빌드 HUD와의 bounding-box overlap 자동 검사 PASS.
- 기존 localStorage 볼륨 저장 유지.

## 4. 공원 배경 재설계

### 산책길

- v1.16의 `pathTileH`, `pathTileV` texture 생성/배치 코드를 제거.
- 고정 seed를 이용한 연속 polygon 리본 2개로 수평/수직 산책길을 그림.
- 바깥 흙 경계 + 안쪽 길면을 분리하고 가장자리에 4px 단위 jitter를 적용.
- 일정 간격으로 잔디 침범과 낮은 대비 마모 흔적을 추가.
- 물리 객체가 아닌 정적 Graphics 1개이므로 충돌과 AI에 영향 없음.

### 중앙 광장

- v1.16의 224×184 대형 네모 `parkPlaza` texture를 제거.
- 중앙에는 약 176×136 외곽 범위의 저대비 팔각형만 그림.
- 내부에는 희미한 발바닥 문양만 남겨 캐릭터보다 먼저 시선을 가져가지 않도록 조정.

### 잔디 / 장식물

- 64×64 잔디 반복 → 256×256 저대비 패턴.
- 큰 grass patch 52개, 소형 장식 150개, 나무 24개 수준으로 제한.
- 벤치/가로등/나무/표지판/울타리/꽃밭/놀이터는 공통 어두운 외곽선 + 제한 팔레트 + 2~3단 명암 규칙으로 다시 그림.
- 장식 수는 v1.16보다 감소.

## 5. 자동 회귀 테스트

### `npm test`

- v1.15 전투 로직: 28 / 28 PASS
- Socket.IO Host-authoritative mock: PASS
- v1.16.1 visual static: PASS

검사 항목에는 pendingTrueBoss, 간식/회복, TRUE BOSS 18개 패턴, 2인 부활, snapshot 필드, 공용 XP/개인 빌드, volatile 전송 관련 기존 로직 보존이 포함됩니다.

### 문법

- `node --check public/game.js`: PASS
- `node --check server.js`: PASS

## 6. Chromium 브라우저 비주얼 검사

`tests/v161_visual_browser_test.py`를 Chromium headless로 실행해 실제 HTML/CSS 레이아웃을 다음 viewport에서 렌더링했습니다.

- 1366×768: PASS
- 1920×1080: PASS
- 2560×1440: PASS

확인 화면:

- 로비
- 전투 HUD + 공원 중앙 프리뷰
- 증강 선택
- ESC 일시정지

자동 확인:

- 로비/ESC player image의 표시 종횡비와 `naturalWidth/naturalHeight` 차이 < 3%
- 6개 캐릭터 이미지 aspect 검사 / viewport PASS
- HUD 최소 safe margin: 1366=18px, 1920=18px, 2560≈20.9px
- 좌하단 BUILD / 우하단 SOUND overlap 없음

브라우저 테스트는 프로젝트의 실제 HTML/CSS를 Chromium으로 렌더링하고, Phaser 월드 영역은 동일 16:9 크기의 정적 공원 프리뷰로 대체합니다. 이 작업 환경에서는 `socket.io` npm dependency를 설치하지 못해 완전한 live Phaser+Socket.IO E2E를 띄우지는 못했습니다. 따라서 최종 Render 배포 후 실제 플레이 화면에서 캐릭터 도트와 보스전까지 한 번 육안 확인하는 것이 마지막 실기 검수입니다.

## 7. 기존 게임 로직 변경 여부

변경하지 않음:

- 전투 밸런스 / 적 HP / 적 속도 / 스폰량
- 경험치 / 레벨 요구량
- 기본 강화 / 특별 증강 / 전용 증강 / Lv.10 증강 수치
- 보물상자 스킬 수치
- 일반 보스 / TRUE BOSS 패턴과 피해 수치
- 회복 / 간식 수치
- 정예 / 정예 변이
- 부활
- Host-authoritative 네트워크 / 공용 XP / 개인 빌드

수정 범위는 플레이어 렌더 배율과 필터, HUD 표시 계층, UI CSS, 배경 Graphics/장식 texture 및 비주얼 상태 표시입니다.

## 남아 있는 위험 요소

1. 1366×768처럼 960×540의 정수 배율이 아닌 viewport에서는 Phaser FIT 자체가 전체 월드 Canvas를 비정수 크기로 표시할 수 있습니다. `autoRound`, NEAREST, 원본 sprite scale 1로 흔들림을 줄였지만 모니터/브라우저 배율 조합마다 픽셀 열 두께가 완전히 동일해질 수는 없습니다.
2. 실제 Render 환경의 GPU/WebGL 드라이버 차이는 headless Chromium 정적 레이아웃 테스트와 다를 수 있습니다.
3. 공원 중앙 외 다른 랜드마크 지역의 최종 미술 체감은 장시간 실제 이동 플레이로 확인하는 것이 가장 정확합니다.
