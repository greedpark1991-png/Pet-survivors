# 멍냥 서바이버즈 v1.16.2 Combat Feel / Collision Hotfix 테스트 보고서

## 목적

v1.16.1의 게임 밸런스와 콘텐츠를 유지한 채 실제 플레이에서 지적된 다음 항목만 수정했다.

1. 플레이어 / 적 탄환 피격판정 신뢰도
2. 포도 TRUE BOSS 돌진 예고와 이동감
3. 전투 공원 배경 / HUD 소규모 마감

공격력, 적 HP, 적 속도, 스폰량, 경험치, 증강 효과, 보물상자 효과, 회복량, 일반 보스 HP, TRUE BOSS HP, 2인 배율은 변경하지 않았다.

---

## 1. Player Hitbox before / after

### v1.16.1

플레이어는 Arcade Physics 사각형 body를 사용했다.

| 캐릭터 | 기존 body | 기존 offset |
|---|---:|---:|
| 찌개 | 14×14 rect | (10,12) |
| 만두 | 13×13 rect | (10,12) |
| 감자 | 14×14 rect | (10,12) |
| 구찌 | 14×14 rect | (10,12) |

64×64 원본 PNG에서 이 offset은 실제 몸통보다 위/왼쪽에 치우쳐 귀·얼굴 근처가 판정 중심처럼 느껴질 수 있었다. 원형 탄막과의 충돌도 rect-vs-circle이어서 모서리 접촉이 체감상 불리할 수 있었다.

### v1.16.2

플레이어 body를 몸통 중심 circle로 변경했다.

| 캐릭터 | 수정 body | 원본 64×64 기준 중심 |
|---|---:|---:|
| 찌개 | radius 7 circle | (31,47) |
| 만두 | radius 7 circle | (31,47) |
| 감자 | radius 6 circle | (33,50) |
| 구찌 | radius 7 circle | (31,49) |

감자는 원래 작은 아기 강아지 체형에 맞춰 radius 6을 유지한다. 구찌의 꼬리, 각 캐릭터의 귀/털 끝/머리 외곽은 핵심 피격 원에 포함하지 않는다.

---

## 2. Enemy Bullet Hitbox before / after

보이는 탄환 texture 크기와 외곽선은 유지했다. 실제 collision core만 더 안쪽으로 옮겼다.

| 탄환 | visual radius | v1.16.1 collision | v1.16.2 collision | 비고 |
|---|---:|---:|---:|---|
| 일반 / 정예탄 | 5 | r4 | **r3.5** | 시각 반경의 70% |
| 일반 보스탄 | 7 | r5 | **r5.0** | 기존도 비교적 정직해 유지 |
| TRUE BOSS탄 | 8 | r6 | **r5.5** | 시각 반경의 68.75% |

모든 적 탄환은 `spawnEnemyBullet()` 한 곳에서 생성되므로 일반 적, 정예, 일반 보스, 포도/초콜릿/양파, Phase 2, 조준탄, 원형탄, 시간차탄 모두 같은 규칙을 사용한다.

플레이어와 탄환이 모두 Arcade circle body를 사용하므로 실제 충돌은 원형 중심 거리 기반으로 처리된다.

### 통로 기하 검증

찌개 r7 + TRUE BOSS탄 r5.5의 합산 충돌 반경은 12.5px이다. TRUE BOSS탄 중심 간격이 29px인 경우 중간 지점에서 각 탄 중심까지 14.5px이므로 충돌 여유가 약 2px 남아 통과한다. 반대로 중심 간격 24px인 통로는 12px로 합산 충돌 반경보다 작아 정상 충돌한다.

즉 보이는 외곽선 사이에 실제로 충분한 공간이 있을 때는 통과하고, 명백히 좁은 통로는 그대로 위험하다.

---

## 3. 개발용 Hitbox Debug

`F2`를 누르면 개발용 hitbox 표시를 ON/OFF 할 수 있다.

- 플레이어: 초록 원
- 일반/정예탄: 빨강 계열 원
- 일반 보스탄: 보라 원
- TRUE BOSS탄: 주황/빨강 원

기본값 OFF이며 일반 DOM UI에는 디버그 버튼이 노출되지 않는다.

---

## 4. Grape Dash before / after

### v1.16.1

- 준비시간: Phase 1 **640ms**, Phase 2 **560ms**
- 예고 그래픽: `rushWarning` 220×14 texture를 `scaleX 1.6`으로 사용한 큰 화살촉/창 형태
- 돌진 방향: 준비 시 예측 각도 고정
- 이동시간: Phase 1 **820ms**, Phase 2 **900ms**
- 이동속도: baseSpeed ×2.85 / ×3.0
- 첫 TRUE BOSS 기준 baseSpeed가 약 63이어서 실제 이동속도는 약 180~189px/s
- 결과적으로 820~900ms 동안 약 **147~170px** 이동하는 짧은 대쉬 느낌
- overshoot endpoint 없음
- 명시적 recovery body state 없음. 다음 공격만 약 550ms 제한
- 돌진 주기: 대략 3.6~5.4초(2P는 더 짧음), Phase 2 패턴이 추가 돌진을 강제할 수 있음

### v1.16.2

- 준비시간: Phase 1 **600ms**, Phase 2 **540ms**
- 예고 그래픽: 화살촉 완전 제거. 바닥 depth 6의 128×8 **얇은 점선 `rushLine`**을 실제 거리 120~360px로 늘려 사용
- 반복 UI 배너 제거. 포도의 몸짓 + 바닥선으로 읽도록 변경
- 준비 모션:
  - 약 6px 뒤로 힘 모으기
  - 후반부 약 ±2px 좌우 떨림
  - 약 ±2.2° 몸 흔들림
  - 마지막 순간 짧은 warm tint
- target lock: 준비 시작 순간의 실제 플레이어 위치를 저장
- 돌진 중 steering: **없음**. 목표가 움직여도 직선 유지
- overshoot: Phase 1 **+52px**, Phase 2 **+60px**
- 최소 거리: 210px
- 최대 거리: **900px**
- 이동시간: **220~280ms**
- 기준 속도: **1650px/s**, 실제 endpoint까지 정확히 도달하도록 거리/시간으로 보정
- 시작: 소량 먼지 6개
- 이동 중: 90ms 간격 짧은 잔상 2~3개 수준
- 종료: 작은 shockwave + 먼지 + 기존 소형 원형탄 후속 공격
- recovery: **380ms** 동안 명시적으로 정지 + 가벼운 흔들림
- 다음 돌진: 약 **6.8~9.2초**, 초기 첫 돌진은 약 6.5~8.5초
- Phase 2의 forced dash도 이 cooldown을 무시하지 못함

일반적인 300~500px 거리에서 포도는 플레이어가 피하지 않으면 목표 지점을 지나쳐 화면을 가르는 공격이 된다. 월드 경계에 너무 가까운 경우에는 월드 밖으로 나가지 않도록 endpoint가 안전 마진에서 clamp된다.

---

## 5. Grape Dash 실제 로직 검증

VM으로 실제 `SurvivorScene.beginRaidRushTelegraph()` / `updateRaidRushState()`를 실행했다.

검증 내용:

- target X/Y가 telegraph 순간 고정됨
- 플레이어가 telegraph 이후 Y축으로 크게 이동해도 dash velocity Y가 변경되지 않음
- Phase 1 endpoint가 원래 목표보다 앞으로 배치됨
- travel time이 220~280ms 안에 있음
- 계산된 실제 dash speed가 1200px/s 이상이며 통상 1600px/s 전후 또는 그 이상
- recovery 상수가 380ms
- 최근 TRUE BOSS 18개 패턴 솔로 / 2P 실행 테스트는 기존대로 모두 통과

---

## 6. 공원 / HUD 소규모 폴리싱

게임 로직과 충돌 오브젝트는 건드리지 않았다.

- 산책길 outer/inner 폭을 한 번 더 축소
- 길/잔디 대비를 낮춘 3단계 흙색 레이어 적용
- 가장자리 잔디 침범 / 마모 픽셀은 낮은 alpha 유지
- 중앙 광장: v1.16.1보다 더 작은 팔각형(outer 약 78×60 logical radius, inner 69×51)으로 축소
- 발바닥 문양 유지, 채도/대비 완화
- 벤치/가로등/표지판은 같은 outline/limited palette/ground shadow 규칙 유지
- 새로운 대량 장식 추가 없음
- 모든 공원 장식은 기존처럼 비물리 배경
- 우하단 사운드 버튼: safe area 유지, hover/outline/SOUND tooltip 확인

---

## 7. 브라우저 시각 검증

Chromium + Playwright로 실제 `index.html` / `styles.css`를 브라우저 엔진에 렌더링하여 UI 및 결정론적 공원 preview를 검사했다.

검사 viewport:

| 해상도 | 최소 HUD safe margin | 캐릭터 원본 aspect ratio 검사 |
|---|---:|---:|
| 1366×768 | 18px | 6개 PASS |
| 1920×1080 | 18px | 6개 PASS |
| 2560×1440 | 20.9px | 6개 PASS |

확인 화면:

- 로비
- 전투 HUD
- 증강 선택
- ESC
- BUILD / SOUND 우하단·좌하단 overlap
- 캐릭터 preview 원본 종횡비
- 공원 중앙 path / plaza / bench / lamp 우선순위

생성 결과는 `tests/screens-v162/` 아래에 저장했다.

### 중요한 제한

이 작업 환경은 외부 네트워크/DNS가 차단되어 있고 프로젝트는 Phaser CDN을 사용한다. 또한 ZIP에는 설치된 `node_modules/socket.io`가 없어서 **실제 Phaser 런타임 + Socket.IO로 Render와 동일한 완전한 1인/2인 보스전을 Chromium에서 실행하는 E2E 테스트는 할 수 없었다.**

따라서 실제 게임 전투 판정/돌진은 JS VM에서 실제 `SurvivorScene` 로직을 직접 실행해 검증했고, 브라우저 검수는 실제 HTML/CSS HUD와 결정론적 맵 시각 preview를 사용했다. Render 배포 후 실제 포도전 한 판의 체감 검수는 최종 사용자 환경에서 한 번 더 확인하는 것이 안전하다.

---

## 8. 자동 테스트 결과

최종 작업 폴더 기준:

- `node --check public/game.js` — PASS
- `node --check server.js` — PASS
- `npm test` — PASS
  - 게임 로직 / 보스 / 회복 / 부활 / pendingTrueBoss / 2P 회귀 테스트: **33 PASS**
  - Socket.IO host-authoritative mock: PASS
  - v1.16.2 combat-feel static test: PASS
- TRUE BOSS 18개 패턴 solo 실행: PASS
- TRUE BOSS 18개 패턴 2P targeting 실행: PASS
- player/raid bullet corridor geometry: PASS
- 포도 target lock / overshoot / straight rush actual state transition: PASS
- DOM id duplicate: 없음
- 핵심 이벤트 listener 중복 정적 검사: PASS

---

## 9. 기존 시스템 보존 확인

다음 데이터 블록은 v1.16.1과 동일함을 비교했다.

- LEVEL_UPGRADES
- 7종 보물상자 SKILLS
- MILESTONE_AUGMENTS
- CHARACTER_AUGMENTS
- MAJOR_AUGMENTS

그리고 다음 시스템의 기존 회귀 테스트가 그대로 통과했다.

- 4캐릭터 / 패시브
- 레벨업 / 특별 / Lv.10 증강
- 보물상자 스킬 성장
- 정예 / 정예 변이
- 후반 scaling
- 일반 보스 성장
- pendingTrueBoss
- 일반 보스 보상 보호
- 간식 / 레벨업 회복
- TRUE BOSS 18개 패턴 / 2페이즈
- TRUE BOSS 처치 보상
- 2인 공용 XP / 개인 빌드
- 부활 50% HP / 2초 무적
- Host-authoritative snapshot
- volatile 네트워크 구조

이번 버전의 의도적인 전투 로직 변경은 **플레이어/적 탄환 collision body와 포도 돌진 움직임/연출**뿐이다.
